require 'spec_helper'

describe CoreTicTacToe do
  it 'has a version number' do
    expect(CoreTicTacToe::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(false)
  end
end
